
export function pushElementToArray(array, element){
    if (Array.isArray(array)){
array.push(element)
    }
    else{
        console.log("It is not an array")
    }
}



export function  shiftArrayElement(array){
    if(Array.isArray(array)){}
    array.shift()
}